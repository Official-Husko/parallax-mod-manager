# Wails build output
build/bin

# Companion executables' own build output (see companions/*/build.sh), and the same
# stray-binary-from-a-plain-`go build` case /parallax-mod-manager above guards against,
# one per companion since each is named after its own folder
companions/*/dist
companions/launcher-shim/launcher-shim

# stray binary from `go build ./...` at repo root
/parallax-mod-manager

# Frontend
frontend/node_modules
frontend/dist

reference-src/
reference-src2/
reference-src3/
reference-src4/


# IDE
.vscode/*
!.vscode/settings.json
CLAUDE.md
mockup/

docs/
*.gobcache
test_old.jsonc
